# קובץ שמגדיר אילו ספריות צריך להתקין כדי שהאפליקציה תעבוד
# כל שורה פה מסבירה מה הספרייה עושה

Flask==2.3.3  # מסגרת לעבודה עם אתרים (האפליקציה הראשית)
Flask-WTF==1.1.1
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Werkzeug==2.3.7  # כלים שמגיעים עם Flask (לא צריך להבין)
itsdangerous==2.1.2  # כלי אבטחה ל-Flask (לחתימות דיגיטליות) - גרסה תואמת
Jinja2==3.1.2  # מערכת תבניות ל-HTML (להצגת דפים)
MarkupSafe==2.1.3  # כלי אבטחה ל-Jinja2 (למניעת התקפות)
click==8.1.7  # כלי ליצירת פקודות (לא חשוב להבין)
blinker==1.6.2  # כלי לאותות ב-Flask (לא חשוב להבין)
matplotlib==3.7.2  # כלי ליצירת גרפים ותרשימים
pandas==2.0.3  # כלי לעבודה עם טבלאות נתונים
numpy==1.24.3  # כלי לעבודה עם מספרים ומערכים
yfinance==0.2.18  # מביא נתונים על מניות מהבורסה
requests==2.31.0  # כלי לשליחת בקשות לאינטרנט
python-dotenv==1.0.0  # כלי לקריאת משתני סביבה מקובץ
gunicorn==21.2.0  # שרת לייצור (להרצה בענן)
email-validator==2.1.0
cryptography==42.0.8
beautifulsoup4==4.12.2
lxml==4.9.3
html5lib==1.1
multitasking==0.0.11
ollama==0.1.7  # חיבור לשירות Ollama (בינה מלאכותית מקומית)
psycopg2-binary==2.9.7  # חיבור למסד נתונים PostgreSQL בענן
SQLAlchemy==2.0.21  # כלי לעבודה עם מסדי נתונים
sniffio==1.3.0  # כלי לזיהוי מערכות הפעלה (נדרש ל-httpx)
httpx==0.25.2  # כלי לשליחת בקשות HTTP (נדרש ל-ollama)
WTForms==3.0.1
