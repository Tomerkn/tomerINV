# .gitignore - קובץ שאומר ל-Git מה לא לשמור
# הקובץ הזה מונע מ-Git לשמור קבצים שאנחנו לא רוצים בקוד המקור
# זה כמו רשימת "אל תיגע" - דברים שצריכים להישאר במחשב שלי בלבד

# למה צריך את זה:
# - קבצים אישיים (סיסמאות, מפתחות)
# - קבצים זמניים שהמחשב יוצר
# - תיקיות שנוצרות אוטומטית
# - קבצים גדולים שלא צריכים להיות בקוד

# קבצים של Python שלא צריכים להישמר
__pycache__/  # תיקיית קבצים זמניים של Python
*.py[cod]     # קבצים מקומפלים של Python
*$py.class    # קבצי קלאס של Python
*.so          # קבצי ספרייה של Python

# קבצי הגדרות אישיות ורגישות
.env          # משתני סביבה רגישים (סיסמאות, מפתחות API)
.env.local    # הגדרות מקומיות
.env.*.local  # הגדרות מקומיות לסביבות שונות
config.py     # קבצי הגדרות שיכולים להכיל מידע רגיש

# קבצי מסד נתונים מקומיים
*.db          # קבצי SQLite
*.sqlite      # קבצי SQLite
*.sqlite3     # קבצי SQLite
instance/     # תיקיית instance עם קבצים מקומיים

# קבצי IDE ועורכי טקסט
.vscode/      # הגדרות Visual Studio Code
.idea/        # הגדרות PyCharm/IntelliJ
*.swp         # קבצים זמניים של Vim
*.swo         # קבצים זמניים של Vim
*~            # קבצי גיבוי אוטומטיים

# קבצי מערכת הפעלה
.DS_Store     # קבצי מערכת של macOS
Thumbs.db     # קבצי מערכת של Windows
*.log         # קבצי לוג
*.tmp         # קבצים זמניים

# קבצי פיתוח ובדיקות
venv/         # סביבה וירטואלית של Python
env/          # סביבה וירטואלית של Python
.pytest_cache/  # קבצי בדיקות
.coverage     # קבצי כיסוי בדיקות
htmlcov/      # דוחות כיסוי HTML

# קבצי Docker ופריסה
.dockerignore  # כבר יש לנו קובץ כזה
docker-compose.override.yml  # הגדרות Docker מקומיות

# קבצי Node.js (אם יש)
node_modules/  # חבילות JavaScript
npm-debug.log  # לוגים של npm
yarn-error.log # לוגים של Yarn

# קבצי גיבוי ותיעוד
*.bak         # קבצי גיבוי
*.backup      # קבצי גיבוי
*.orig        # קבצים מקוריים
*.rej         # קבצי דחייה של Git

# קבצי אבטחה ואישורים
*.pem         # מפתחות SSL
*.key         # מפתחות הצפנה
*.crt         # אישורי SSL
*.p12         # קבצי אישורים
credentials.json  # קבצי אישורים של Google/Firebase

# זה הכל! עכשיו Git יודע מה לא לשמור
# הקובץ הזה עוזר לשמור על הקוד נקי ובטוח

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Database files
*.db
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment variables
.env
.env.local
.env.production

# Temporary files
*.tmp
*.temp 